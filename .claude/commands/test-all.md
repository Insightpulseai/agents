Run the full test suite across all test types:

1. Unit tests: `python -m pytest tests/unit/ -v`
2. Integration tests: `python -m pytest tests/integration/ -v`
3. Agent capability tests: `python -m pytest tests/test_agent_capability_matrix.py -v`

Report pass/fail summary with any failures explained.
